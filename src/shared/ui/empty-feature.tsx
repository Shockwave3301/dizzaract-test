import { type ReactElement } from "react";

function EmptyFeature(): ReactElement {
  return (
    <p className="m-auto text-center text-muted-foreground">
      There is no feature here (yet).
    </p>
  );
}

export default EmptyFeature;
